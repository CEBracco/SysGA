{% extends 'base.html.twig' %}

{% block body %}
    <h1>Registrodelautomotor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registroDelAutomotor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ registroDelAutomotor.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('registrodelautomotor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('registrodelautomotor_edit', { 'id': registroDelAutomotor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
